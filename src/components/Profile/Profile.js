import React from 'react';
import { Container } from '@material-ui/core';
const Profile = () => {
    return (
        <Container>
            <h4>Learning will be available</h4>
        </Container>
    );
};

export default Profile;